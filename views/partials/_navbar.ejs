<nav class="navbar">
  <div>
    Sociometric Test Results Calculator
  </div>
    <div class="navbar-links">
      <% if (user) { %>
        <a href="/users/<%= user._id %>/assessments">My Assessments</a>
        <a href="/auth/sign-out">Sign Out</a>
      <% } else { %>
        <a href="/auth/sign-in">Sign In</a>
        <a href="/auth/sign-up">Sign Up</a>
      <% } %>
    </div>
  </nav>